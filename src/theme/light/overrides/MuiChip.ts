import { colors } from "@mui/material";

export default {
    styleOverrides: {
        deletable: {
            '&:focus': {
                backgroundColor: colors.blueGrey[100],
            },
        },
    }
}
